// import React from 'react';
// import { ScrollArea } from "../components/ui/scroll-area";
// import { ChatMessage } from './ChatMessage';

// interface MessageScrollAreaProps {
//   messages: { role: string; content: string; isLoading?: boolean }[];
// }

// export const MessageScrollArea: React.FC<MessageScrollAreaProps> = ({ messages }) => {
//   return (
//     <ScrollArea className="h-[400px] p-4 scroll-area">
//       {messages.map((message, index) => (
//         <ChatMessage key={index} role={message.role} content={message.content} isLoading={message.isLoading} />
//       ))}
//     </ScrollArea>
//   );
// }
